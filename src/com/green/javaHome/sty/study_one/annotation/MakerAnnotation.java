package com.green.javaHome.sty.study_one.annotation;

public class MakerAnnotation {
    public static void main(String[] args) {
        /*
        마커 애너테이션
        -요소가 하나도 정의 되지 않은 애너테이션

        애너테이션 요소의 규칙
        -애너테이션의 요소를 선언 할 때 아래의 규칙을 반드시 지켜야 한다.
          -> 요소의 타입은 기본형,String, enum, 애너테이션, Class만 허용됨
          -> 괄호() 안에 매개변수 선언할 수 없다.
          -> 예외를 선언할 수 없다.
          -> 요소 타입 매개변수로 정의 할 수 없다.
         */
    }
}
